@model ProyectoAnimales.Models.Animale

@{
    ViewData["Title"] = "Confirmar Eliminación"; // More descriptive title
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container-fluid mt-4">
    @* Add container for better spacing and responsiveness *@
    <div class="card shadow-sm border-danger">
        @* Use a card with a danger border for emphasis *@
        <div class="card-header bg-danger text-white">
            @* Header with danger background and white text *@
            <h1 class="card-title text-center mb-0"><i class="bi bi-exclamation-triangle"></i> Eliminar Animal</h1> @* Centered title with warning icon *@
        </div>
        <div class="card-body">
            <h3 class="text-center text-danger mb-4">¿Estás seguro de que quieres eliminar este registro?</h3> @* Prominent confirmation message *@
            <p class="text-center text-muted">Esta acción es irreversible y eliminará los datos de **@Model.Nombre**.</p> @* Additional warning *@
            <hr class="my-4" /> @* Horizontal rule for separation *@

            <h4 class="mb-3 text-center">Detalles del Animal</h4> @* Subtitle for details *@
            <dl class="row dl-horizontal">
                @* Use dl-horizontal for Bootstrap's description list styling *@
                <dt class="col-sm-3 text-end">
                    @* Align labels to the right *@
                    @Html.DisplayNameFor(model => model.Nombre)
                </dt>
                <dd class="col-sm-9">
                    @Html.DisplayFor(model => model.Nombre)
                </dd>

                <dt class="col-sm-3 text-end">
                    @Html.DisplayNameFor(model => model.Especie)
                </dt>
                <dd class="col-sm-9">
                    @Html.DisplayFor(model => model.Especie)
                </dd>

                <dt class="col-sm-3 text-end">
                    @Html.DisplayNameFor(model => model.Edad)
                </dt>
                <dd class="col-sm-9">
                    @Html.DisplayFor(model => model.Edad)
                </dd>

                <dt class="col-sm-3 text-end">
                    @Html.DisplayNameFor(model => model.Propietario)
                </dt>
                <dd class="col-sm-9">
                    @Html.DisplayFor(model => model.Propietario.PropietarioId)
                </dd>
            </dl>
        </div>
        <div class="card-footer text-center bg-light d-flex justify-content-center gap-3">
            @* Footer for card, light background, centered buttons with gap *@
            <form asp-action="Delete">
                <input type="hidden" asp-for="AnimalId" />
                <button type="submit" class="btn btn-danger btn-lg">
                    @* Prominent danger button for deletion *@
                    <i class="bi bi-trash"></i> Confirmar Eliminación
                </button>
            </form>
            <a asp-action="Index" class="btn btn-outline-secondary btn-lg">
                @* Outline button for back link, larger for consistency *@
                <i class="bi bi-arrow-left-circle"></i> Cancelar y Volver a la Lista
            </a>
        </div>
    </div>
</div>